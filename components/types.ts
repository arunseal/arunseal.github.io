export type TextWithImage = {
  text: string;
  imageSrc: string;
  bgEnabled: boolean;
};

export type TextWithoutImage = {
  text: string;
};

export type Publication = {
  text: string;
  link: string;
};
